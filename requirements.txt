# Núcleo da API
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Banco de dados (ORM) - mantém o código independente do SGBD.
# O SQLite (padrão para rodar localmente) já vem embutido no Python, então não
# precisa de driver extra. Para usar PostgreSQL, instale também o psycopg2-binary:
#     pip install psycopg2-binary
SQLAlchemy==2.0.30

# Validação e modelagem dos dados de entrada/saída
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.1.1

# Segurança: hash de senha (bcrypt) e geração/leitura de tokens JWT
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0

# Testes
pytest==8.2.2
httpx==0.27.0
